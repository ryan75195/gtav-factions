namespace FactionWars.Combat.Models
{
    /// <summary>
    /// A zone combatant's stance toward the player: friendly (same faction as the player)
    /// or hostile (any other faction).
    /// </summary>
    public enum Allegiance
    {
        Friendly,
        Hostile
    }
}
